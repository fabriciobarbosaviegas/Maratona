#include <stdio.h>
#include <string.h>

int main(){
    int vogais[5] = {'a', 'e', 'i', 'o', 'u'};    
    char palavra[30], novaPalavra[90];

    scanf('%s', palavra);

    for(int i = 0; i < strlen(palavra); i++){
        
    }

    return 0;
}
